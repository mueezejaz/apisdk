{
  "name": "aisdk",
  "version": "1.0.0",
  "private": true,
  "description": "Dev workspace for gemini-lb (local dashboard + tests)",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dashboard": "tsx dashboard/main.ts",
    "test:tokenharbor": "tsx scripts/token-harbor-smoke.ts",
    "typecheck": "tsc --noEmit && npm run typecheck --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@ai-sdk/provider-utils": "^5.0.36",
    "@types/node": "^26.4.1",
    "ai": "^7.0.92",
    "cheerio": "^1.2.0",
    "dotenv": "^18.0.3",
    "express": "^5.2.1",
    "typescript": "^7.0.2",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "tsx": "^4.23.15"
  }
}
